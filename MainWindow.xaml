<Window x:Class="CrossoutDBUploader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CrossoutDB Combat Uploader" Height="500" Width="520" Icon="pack://application:,,,/app.ico">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

		<StackPanel Margin="10">

			<TextBlock Text="API URL"/>
			<TextBox x:Name="ApiUrlBox" Height="25"/>

			<TextBlock Text="Logs folder" Margin="0,10,0,0"/>
			<StackPanel Orientation="Horizontal">
				<TextBox x:Name="LogFolderBox" Height="25" Width="410"/>
				<Button Content="Browse..." Click="BrowseLogFolder_Click" Width="80" Margin="10,0,0,0"/>
			</StackPanel>

			<TextBlock Text="API Key" Margin="0,10,0,0"/>
			<PasswordBox x:Name="ApiKeyBox" Height="25"/>

			<StackPanel Orientation="Horizontal" Margin="0,10,0,10">
				<Button Content="Save" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
				<Button Content="Rescan" Click="Rescan_Click" Width="80" Margin="0,0,10,0"/>
				<Button Content="Send" Click="SendLogs_Click" Width="80"/>
			</StackPanel>

			<CheckBox Content="Auto send every 60s"
					  x:Name="AutoSendCheckBox"
					  Checked="AutoSend_Checked"
					  Unchecked="AutoSend_Unchecked"
					  Margin="0,0,0,10"/>

			<CheckBox x:Name="AutoStartCheckBox"
					  Content="Start with Windows"
					  Margin="0,0,0,10"/>

			<TextBox x:Name="LogBox"
					 AcceptsReturn="True"
					 VerticalScrollBarVisibility="Auto"
					 Height="250"/>
		</StackPanel>
    </Grid>
</Window>
